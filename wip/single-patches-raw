Signal-out -> Total
Signal-out -> Freq
Signal-out -> Ext-signal-in
Signal-out -> VCHPF-cutoff-in
Signal-out -> VCLPF-cutoff-in
Signal-out -> VCA-initial
Signal-out -> VCO1+2-CV-in
Signal-out -> VCO2-CV-in
Signal-out -> S&H-clock-in
Signal-out -> VCA-CV-in
Signal-out -> EG1-trigger-in
Signal-out -> Trig-in
Signal-out -> S&H-in
Signal-out -> VCA-in
Signal-out -> ESP-signal-in
Headphone-out -> Total
Headphone-out -> Freq
Headphone-out -> Ext-signal-in
Headphone-out -> VCHPF-cutoff-in
Headphone-out -> VCLPF-cutoff-in
Headphone-out -> VCA-initial
Headphone-out -> VCO1+2-CV-in
Headphone-out -> VCO2-CV-in
Headphone-out -> S&H-clock-in
Headphone-out -> VCA-CV-in
Headphone-out -> EG1-trigger-in
Headphone-out -> Trig-in
Headphone-out -> S&H-in
Headphone-out -> VCA-in
Headphone-out -> ESP-signal-in
MG-triangle-out -> Total
MG-triangle-out -> Freq
MG-triangle-out -> Ext-signal-in
MG-triangle-out -> VCHPF-cutoff-in
MG-triangle-out -> VCLPF-cutoff-in
MG-triangle-out -> VCA-initial
MG-triangle-out -> VCO1+2-CV-in
MG-triangle-out -> VCO2-CV-in
MG-triangle-out -> S&H-clock-in
MG-triangle-out -> VCA-CV-in
MG-triangle-out -> EG1-trigger-in
MG-triangle-out -> Trig-in
MG-triangle-out -> S&H-in
MG-triangle-out -> VCA-in
MG-triangle-out -> ESP-signal-in
MG-pulse-out -> Total
MG-pulse-out -> Freq
MG-pulse-out -> Ext-signal-in
MG-pulse-out -> VCHPF-cutoff-in
MG-pulse-out -> VCLPF-cutoff-in
MG-pulse-out -> VCA-initial
MG-pulse-out -> VCO1+2-CV-in
MG-pulse-out -> VCO2-CV-in
MG-pulse-out -> S&H-clock-in
MG-pulse-out -> VCA-CV-in
MG-pulse-out -> EG1-trigger-in
MG-pulse-out -> Trig-in
MG-pulse-out -> S&H-in
MG-pulse-out -> VCA-in
MG-pulse-out -> ESP-signal-in
EG1-out -> Total
EG1-out -> Freq
EG1-out -> Ext-signal-in
EG1-out -> VCHPF-cutoff-in
EG1-out -> VCLPF-cutoff-in
EG1-out -> VCA-initial
EG1-out -> VCO1+2-CV-in
EG1-out -> VCO2-CV-in
EG1-out -> S&H-clock-in
EG1-out -> VCA-CV-in
EG1-out -> EG1-trigger-in
EG1-out -> Trig-in
EG1-out -> S&H-in
EG1-out -> VCA-in
EG1-out -> ESP-signal-in
EG2-rev-out -> Total
EG2-rev-out -> Freq
EG2-rev-out -> Ext-signal-in
EG2-rev-out -> VCHPF-cutoff-in
EG2-rev-out -> VCLPF-cutoff-in
EG2-rev-out -> VCA-initial
EG2-rev-out -> VCO1+2-CV-in
EG2-rev-out -> VCO2-CV-in
EG2-rev-out -> S&H-clock-in
EG2-rev-out -> VCA-CV-in
EG2-rev-out -> EG1-trigger-in
EG2-rev-out -> Trig-in
EG2-rev-out -> S&H-in
EG2-rev-out -> VCA-in
EG2-rev-out -> ESP-signal-in
KBD-CV-out -> Total
KBD-CV-out -> Freq
KBD-CV-out -> Ext-signal-in
KBD-CV-out -> VCHPF-cutoff-in
KBD-CV-out -> VCLPF-cutoff-in
KBD-CV-out -> VCA-initial
KBD-CV-out -> VCO1+2-CV-in
KBD-CV-out -> VCO2-CV-in
KBD-CV-out -> S&H-clock-in
KBD-CV-out -> VCA-CV-in
KBD-CV-out -> EG1-trigger-in
KBD-CV-out -> Trig-in
KBD-CV-out -> S&H-in
KBD-CV-out -> VCA-in
KBD-CV-out -> ESP-signal-in
EG1-rev-out -> Total
EG1-rev-out -> Freq
EG1-rev-out -> Ext-signal-in
EG1-rev-out -> VCHPF-cutoff-in
EG1-rev-out -> VCLPF-cutoff-in
EG1-rev-out -> VCA-initial
EG1-rev-out -> VCO1+2-CV-in
EG1-rev-out -> VCO2-CV-in
EG1-rev-out -> S&H-clock-in
EG1-rev-out -> VCA-CV-in
EG1-rev-out -> EG1-trigger-in
EG1-rev-out -> Trig-in
EG1-rev-out -> S&H-in
EG1-rev-out -> VCA-in
EG1-rev-out -> ESP-signal-in
KBD-trig-out -> Total
KBD-trig-out -> Freq
KBD-trig-out -> Ext-signal-in
KBD-trig-out -> VCHPF-cutoff-in
KBD-trig-out -> VCLPF-cutoff-in
KBD-trig-out -> VCA-initial
KBD-trig-out -> VCO1+2-CV-in
KBD-trig-out -> VCO2-CV-in
KBD-trig-out -> S&H-clock-in
KBD-trig-out -> VCA-CV-in
KBD-trig-out -> EG1-trigger-in
KBD-trig-out -> Trig-in
KBD-trig-out -> S&H-in
KBD-trig-out -> VCA-in
KBD-trig-out -> ESP-signal-in
S&H-out -> Total
S&H-out -> Freq
S&H-out -> Ext-signal-in
S&H-out -> VCHPF-cutoff-in
S&H-out -> VCLPF-cutoff-in
S&H-out -> VCA-initial
S&H-out -> VCO1+2-CV-in
S&H-out -> VCO2-CV-in
S&H-out -> S&H-clock-in
S&H-out -> VCA-CV-in
S&H-out -> EG1-trigger-in
S&H-out -> Trig-in
S&H-out -> S&H-in
S&H-out -> VCA-in
S&H-out -> ESP-signal-in
VCA-out -> Total
VCA-out -> Freq
VCA-out -> Ext-signal-in
VCA-out -> VCHPF-cutoff-in
VCA-out -> VCLPF-cutoff-in
VCA-out -> VCA-initial
VCA-out -> VCO1+2-CV-in
VCA-out -> VCO2-CV-in
VCA-out -> S&H-clock-in
VCA-out -> VCA-CV-in
VCA-out -> EG1-trigger-in
VCA-out -> Trig-in
VCA-out -> S&H-in
VCA-out -> VCA-in
VCA-out -> ESP-signal-in
Pink -> Total
Pink -> Freq
Pink -> Ext-signal-in
Pink -> VCHPF-cutoff-in
Pink -> VCLPF-cutoff-in
Pink -> VCA-initial
Pink -> VCO1+2-CV-in
Pink -> VCO2-CV-in
Pink -> S&H-clock-in
Pink -> VCA-CV-in
Pink -> EG1-trigger-in
Pink -> Trig-in
Pink -> S&H-in
Pink -> VCA-in
Pink -> ESP-signal-in
White -> Total
White -> Freq
White -> Ext-signal-in
White -> VCHPF-cutoff-in
White -> VCLPF-cutoff-in
White -> VCA-initial
White -> VCO1+2-CV-in
White -> VCO2-CV-in
White -> S&H-clock-in
White -> VCA-CV-in
White -> EG1-trigger-in
White -> Trig-in
White -> S&H-in
White -> VCA-in
White -> ESP-signal-in
Wheel -> Total
Wheel -> Freq
Wheel -> Ext-signal-in
Wheel -> VCHPF-cutoff-in
Wheel -> VCLPF-cutoff-in
Wheel -> VCA-initial
Wheel -> VCO1+2-CV-in
Wheel -> VCO2-CV-in
Wheel -> S&H-clock-in
Wheel -> VCA-CV-in
Wheel -> EG1-trigger-in
Wheel -> Trig-in
Wheel -> S&H-in
Wheel -> VCA-in
Wheel -> ESP-signal-in
Momentary -> Total
Momentary -> Freq
Momentary -> Ext-signal-in
Momentary -> VCHPF-cutoff-in
Momentary -> VCLPF-cutoff-in
Momentary -> VCA-initial
Momentary -> VCO1+2-CV-in
Momentary -> VCO2-CV-in
Momentary -> S&H-clock-in
Momentary -> VCA-CV-in
Momentary -> EG1-trigger-in
Momentary -> Trig-in
Momentary -> S&H-in
Momentary -> VCA-in
Momentary -> ESP-signal-in
ESP-signal-out -> Total
ESP-signal-out -> Freq
ESP-signal-out -> Ext-signal-in
ESP-signal-out -> VCHPF-cutoff-in
ESP-signal-out -> VCLPF-cutoff-in
ESP-signal-out -> VCA-initial
ESP-signal-out -> VCO1+2-CV-in
ESP-signal-out -> VCO2-CV-in
ESP-signal-out -> S&H-clock-in
ESP-signal-out -> VCA-CV-in
ESP-signal-out -> EG1-trigger-in
ESP-signal-out -> Trig-in
ESP-signal-out -> S&H-in
ESP-signal-out -> VCA-in
ESP-signal-out -> ESP-signal-in
ESP-BPF-out -> Total
ESP-BPF-out -> Freq
ESP-BPF-out -> Ext-signal-in
ESP-BPF-out -> VCHPF-cutoff-in
ESP-BPF-out -> VCLPF-cutoff-in
ESP-BPF-out -> VCA-initial
ESP-BPF-out -> VCO1+2-CV-in
ESP-BPF-out -> VCO2-CV-in
ESP-BPF-out -> S&H-clock-in
ESP-BPF-out -> VCA-CV-in
ESP-BPF-out -> EG1-trigger-in
ESP-BPF-out -> Trig-in
ESP-BPF-out -> S&H-in
ESP-BPF-out -> VCA-in
ESP-BPF-out -> ESP-signal-in
ESP-FVCV-out -> Total
ESP-FVCV-out -> Freq
ESP-FVCV-out -> Ext-signal-in
ESP-FVCV-out -> VCHPF-cutoff-in
ESP-FVCV-out -> VCLPF-cutoff-in
ESP-FVCV-out -> VCA-initial
ESP-FVCV-out -> VCO1+2-CV-in
ESP-FVCV-out -> VCO2-CV-in
ESP-FVCV-out -> S&H-clock-in
ESP-FVCV-out -> VCA-CV-in
ESP-FVCV-out -> EG1-trigger-in
ESP-FVCV-out -> Trig-in
ESP-FVCV-out -> S&H-in
ESP-FVCV-out -> VCA-in
ESP-FVCV-out -> ESP-signal-in
ESP-EFCV-out -> Total
ESP-EFCV-out -> Freq
ESP-EFCV-out -> Ext-signal-in
ESP-EFCV-out -> VCHPF-cutoff-in
ESP-EFCV-out -> VCLPF-cutoff-in
ESP-EFCV-out -> VCA-initial
ESP-EFCV-out -> VCO1+2-CV-in
ESP-EFCV-out -> VCO2-CV-in
ESP-EFCV-out -> S&H-clock-in
ESP-EFCV-out -> VCA-CV-in
ESP-EFCV-out -> EG1-trigger-in
ESP-EFCV-out -> Trig-in
ESP-EFCV-out -> S&H-in
ESP-EFCV-out -> VCA-in
ESP-EFCV-out -> ESP-signal-in
ESP-trig-out -> Total
ESP-trig-out -> Freq
ESP-trig-out -> Ext-signal-in
ESP-trig-out -> VCHPF-cutoff-in
ESP-trig-out -> VCLPF-cutoff-in
ESP-trig-out -> VCA-initial
ESP-trig-out -> VCO1+2-CV-in
ESP-trig-out -> VCO2-CV-in
ESP-trig-out -> S&H-clock-in
ESP-trig-out -> VCA-CV-in
ESP-trig-out -> EG1-trigger-in
ESP-trig-out -> Trig-in
ESP-trig-out -> S&H-in
ESP-trig-out -> VCA-in
ESP-trig-out -> ESP-signal-in
